document.addEventListener("DOMContentLoaded", function() {
    // var marginTopStr = window.getComputedStyle(document.querySelector('.item')).marginTop;
    // var indexPX = marginTopStr.indexOf('px');
    // var marginTopNum = Math.round(marginTopStr.substring(0, indexPX));
    // var list = document.querySelectorAll('.item');
    // for (var i = 0; i < list.length; i++) {
    //     list[i].style.marginTop = marginTopNum + 'px'
    // }
    // alert(2);
}, false);